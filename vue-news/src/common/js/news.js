export default class News{
    constructor({id,title,content,imgs,tags}){
        this.id=id
        this.title=title
        this.content=content,
        this.imgs=imgs,
        this.tags=tags
    }
}